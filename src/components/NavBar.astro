import { navBarLinks } from "../config";
import { Icon } from "astro-icon/components";
import type { NavBarLink } from "../types/config";

<nav class="fixed top-0 left-0 right-0 z-50 bg-gray-900/80 backdrop-blur-md border-b border-white/10">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <a href="/" class="flex items-center gap-2 text-white hover:text-blue-400 transition-colors">
        <Icon name="mdi:home" class="w-6 h-6" />
        <span class="font-medium">Home</span>
      </a>
      <div class="flex items-center gap-6">
        {navBarLinks.map((link: NavBarLink) => (
          <a
            href={link.url}
            class="text-gray-400 hover:text-white transition-colors"
            target={link.external ? "_blank" : "_self"}
            rel={link.external ? "noopener noreferrer" : undefined}
          >
            {link.title}
          </a>
        ))}
      </div>
    </div>
  </div>
</nav> 